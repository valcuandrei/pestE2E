<?php

namespace Tests;

abstract class E2ETestCase extends TestCase
{
    /**
     * Default package manager for E2E runs (npm, yarn, pnpm, bun).
     * Override via --run-using= or config('pest-e2e.package_manager').
     */
    protected static string $e2ePackageManager = '{{PACKAGE_MANAGER}}';

    /**
     * Register e2e targets.
     *
     * Re-register on every test: Laravel refreshes the container between tests,
     * so a static "already registered" guard would leave an empty TargetRegistry.
     */
    protected function setUp(): void
    {
        $this->markAsE2ETest();
        parent::setUp();

        if (config('pest-e2e.package_manager') === null) {
            config(['pest-e2e.package_manager' => self::$e2ePackageManager]);
        }

        e2e()->target('frontend', fn ($p) => $p
            ->dir('resources/js/e2e')
            ->env(['APP_URL' => 'http://localhost'])
            ->params(['baseUrl' => 'http://localhost'])
        );
    }

    /**
     * Mark the test as an E2E test.
     */
    private function markAsE2ETest(): void
    {
        $_ENV['IS_E2E_TEST'] = true;
    }
}
